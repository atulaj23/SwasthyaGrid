import {NextResponse} from 'next/server';import {approveRecommendation,recommendations} from '@/lib/operations';export const dynamic='force-dynamic';export async function GET(){return NextResponse.json(recommendations())}export async function POST(req:Request){const {id,action}=await req.json();if(action!=='ACCEPT')return NextResponse.json({id,status:'REJECTED'});const result=approveRecommendation(id);return result?NextResponse.json(result):NextResponse.json({error:'Recommendation is no longer valid or source stock is insufficient.'},{status:409})}
